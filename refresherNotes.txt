Function Call/Variable syntax

[_player, _one, _two] execVM "path/to/script.sqf";

params ["_player". "_one", "_two"]

Not sure why our references of _varName did not work

But

_player = _this select 0;
_one = _this select 1;

syntax works for sure